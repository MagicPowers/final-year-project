public class alphabetFrequencies
{
	char let; // the letter
	int frequency; // the frequency associated with that letter
	
	public alphabetFrequencies(char l, int f)
	{
		let = l;
		frequency = f;
	}
}
